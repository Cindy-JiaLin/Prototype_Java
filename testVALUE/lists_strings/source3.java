package string;

public class StringDiff 
{ // Converting from string a to string b which means
  // insert operations refer to characters that are in b, but not in the matching position in a
  // delete operations refer to characters that are in a, but not in the matching position in b
}
